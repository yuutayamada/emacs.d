;;; -*- coding: utf-8; mode: emacs-lisp; -*-

;;; Commentary:
;;; Code:

;; need to fix the recipe of ac-slime (add auto-complete as :depends)
;; (require 'slime)
;; (require 'ac-slime)
;; (add-hook 'slime-mode-hook 'set-up-slime-ac)
;; (add-hook 'slime-repl-mode-hook 'set-up-slime-ac)

(provide 'init-ac-slime)

;; Local Variables:
;; coding: utf-8
;; mode: emacs-lisp
;; End:

;;; init-ac-slime.el ends here
