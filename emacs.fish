# This file intrigues to adjust fish shell in Emacs
# Probably, you may need kinda following code:
#   if test -n "$EMACS"; or test -n "$INSIDE_EMACS"
#     source path/to/this/file
#   end
# Note: the $INSIDE_EMACS environment variable introduced in Emacs 25.

# from FAQ page
function fish_title; end; funcsave fish_title

