;;; default.el --- default file

;;; Commentary:
;; Actually this is fallback file because there is no way to specify option to
;; `emacsclient -t -a emacs'
;;; Code:

(require 'init nil t)

(provide 'default)

;; Local Variables:
;; no-byte-compile: t
;; coding: utf-8
;; mode: emacs-lisp
;; End:

;;; default.el ends here
