;;; -*- coding: utf-8; mode: emacs-lisp; -*-

;;; Code:

(require 'Y-autoload)
;;;###autoload
(el-get 'sync 'lua-mode)

(require 'lua-mode)
(setq lua-indent-level 2)

(provide 'init_lua-mode)

;; Local Variables:
;; coding: utf-8
;; mode: emacs-lisp
;; End:

;;; init_lua-mode.el ends here
